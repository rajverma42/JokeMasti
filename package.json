{
  "name": "jokemasti",
  "version": "1.0.0",
  "private": true,
  "description": "JokeMasti — Hasi Ka Daily Dose! A fast, static, SEO-optimised Hindi/Hinglish/English jokes platform for Indian users.",
  "type": "module",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node build.js",
    "images": "python3 scripts/generate_images.py",
    "serve": "python3 -m http.server 8080 --directory dist",
    "check": "node scripts/check-links.js"
  },
  "engines": {
    "node": ">=18"
  }
}
